# EHL refactor log

- Converted the project from legacy `BBBPluginDefaults.cmake` and sibling JUCE assumptions to a self-contained CMake/JUCE FetchContent build.
- Updated identity to EsionHsrahLatigid, `EHL_`, `jp.ehl.lameglitch`, and preserved existing plug-in code `LmGl`.
- Added artifact staging for VST3, AU, and Standalone under `artifacts/<CONFIG>/`.
- Added `LAMEglitchSmokeTests` to instantiate and process a finite block in simulation mode.
- Added reusable EHL GitHub Actions CI using `EsionHsrahLatigid/juce-ci`.
- Removed generated build output, `.DS_Store`, and the obsolete `build_mac.sh` script before validation.
- Local validation on 2026-08-12:
  - `check_juce_project.py` passed.
  - `cmake -S . -B build/release -DCMAKE_BUILD_TYPE=Release -DLAMEGLITCH_BUILD_PLUGIN=ON -DLAMEGLITCH_BUILD_TESTS=ON` passed.
  - `cmake --build build/release --target LAMEglitch_Artifacts LAMEglitchSmokeTests --parallel 2` passed.
  - `ctest --test-dir build/release --output-on-failure` passed.
